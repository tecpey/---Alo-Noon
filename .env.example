NODE_ENV=development
API_PORT=3001
API_VERSION=v1
CORS_ORIGINS=http://localhost:3000,http://localhost:8081
EXPO_PUBLIC_API_BASE_URL=http://localhost:3001
DATABASE_URL=postgresql://alo_noon:alo_noon@localhost:5432/alo_noon?schema=public
# Generate independent values with a secrets manager; required in production.
AUTH_OTP_PEPPER=
AUTH_SESSION_PEPPER=
AUTH_ABUSE_PEPPER=
# Number of trusted reverse-proxy hops in front of the API. Required in production:
# rate limiting and OTP abuse control key on the client IP, so getting this wrong
# behind a load balancer puts every user in one shared bucket.
#
# The address of the proxy, not a count of proxies: "loopback" when nginx runs on
# this host, otherwise its IP or CIDR block (comma-separated for several). Use
# "none" only when the API is exposed directly. A hop count is not accepted --
# counting hops cannot identify the immediate peer, so a client connecting
# directly could forge X-Forwarded-For and be believed.
API_TRUST_PROXY=none
# OTP delivery credentials. The database stores only the opaque reference
# (env://AUTH_SMS_LIMOSMS_KEY); the key itself lives here and is never committed.
# The variable name must match the reference the SMS configuration was created
# with, and every such reference has to start with AUTH_SMS_.
AUTH_SMS_LIMOSMS_KEY=
# Optional. Error reporting stays fully disabled until this is set.
SENTRY_DSN=
# A pair, and the server treats them as one: no gateway adapter is registered
# unless both are set. The first is the address adapters hand the gateway to send
# the customer back to; the second is what makes the route at that address exist.
# Half the pair would send a paying customer to a 404 with the card already
# debited, so half the pair disables online payment instead.
PAYMENT_CALLBACK_BASE_URL=http://localhost:3001
# Where the customer lands after returning from the gateway. Carries no payment
# verdict, so a forged callback cannot make the app show a successful payment.
PAYMENT_RESULT_REDIRECT_URL=http://localhost:3000/payments/result
# Base64 32-byte key that opens local-encrypted:// payment credentials. Generate
# with: pnpm --filter @alo-noon/api provision generate-encryption-key
# Keep it somewhere other than the encrypted values below, or encryption is moot.
PAYMENT_SECRET_ENCRYPTION_KEY=
# One per gateway: a local-encrypted://IDPAY reference reads PAYMENT_SECRET_IDPAY.
# Store the ciphertext from `provision encrypt-payment-secret`, never the raw key.
PAYMENT_SECRET_IDPAY=
# Zarinpal's credential is its merchant id, encrypted the same way:
# {"merchantId":"<uuid from the Zarinpal panel>"}
PAYMENT_SECRET_ZARINPAL=
# Gateway origin the Zarinpal adapter talks to, keeping Zarinpal's own paths.
# Leave empty and a TEST configuration uses Zarinpal's real sandbox while a
# PRODUCTION one uses the live gateway — which is what both want. Set it only to
# point at a stand-in when the sandbox is unreachable from your network:
#   pnpm --filter @alo-noon/api exec tsx scripts/zarinpal-sandbox.ts
# Never set this in production.
PAYMENT_ZARINPAL_ENDPOINT=
# Zibal's credential is its merchant key: {"merchant":"<key from the Zibal panel>"}
PAYMENT_SECRET_ZIBAL=
# Zibal publishes no separate sandbox host — one set of endpoints serves both
# environments — so this override is the only way to exercise it without a live
# merchant. Leave empty in production.
PAYMENT_ZIBAL_ENDPOINT=
# Where the admin panel reaches the API. Must carry the TENANT's host, not an
# internal address: the API resolves the tenant from the host a request arrives
# on, and Node's fetch will not let the panel override the Host header. Point
# this at 127.0.0.1 and no tenant resolves, so the panel loops back to login.
ADMIN_API_BASE_URL=http://localhost:3001

# --- Routing -----------------------------------------------------------------
# The Neshan API key, behind the env://ROUTING_NESHAN_KEY reference a routing
# configuration stores. Paste the key itself: unlike a payment credential this
# is not JSON, because a routing engine needs exactly one secret.
# Without it, delivery distance falls back to the scaled straight line — orders
# still go through, and every quote records that its fare was estimated.
ROUTING_NESHAN_KEY=
# Where the Neshan adapter asks. Empty means the real service. Set only to point
# at a local stand-in during development.
ROUTING_NESHAN_ENDPOINT=

# --- Delivery fare (who prices the courier) ----------------------------------
# Tapsi Pack's credential, behind the env://DELIVERY_FARE_TAPSI_PACK reference a
# fare configuration stores. Two fields, as JSON, because Tapsi authenticates
# with both a secret key and a user id:
#   {"secretKey":"<from the Tapsi Pack panel>","userId":"<numeric user id>"}
# The adapter base64-encodes the user id itself; paste it as the plain number.
#
# Without it, delivery is priced on the published tariff — orders still go
# through and every quote records that its fare came from the tariff.
DELIVERY_FARE_TAPSI_PACK=
# Where the Tapsi adapter asks. Empty means the real service.
TAPSI_PACK_ENDPOINT=
# What Tapsi's amounts are denominated in, and what its dateTimestamp expects.
#
# Both default to RIAL and MILLISECONDS. Neither is in Tapsi's published
# specification, which is archived and incomplete, so both are stated
# assumptions rather than facts.
#
# RIAL against TOMAN is a factor of ten. Place one real order and reconcile the
# fare against the invoice Tapsi issues BEFORE marking the configuration
# HEALTHY — until it is HEALTHY nothing is quoted through it, which is what
# makes that check safe to do.
TAPSI_PACK_AMOUNT_UNIT=
TAPSI_PACK_TIMESTAMP_UNIT=

# Snapp Box's credential, behind the env://DELIVERY_FARE_SNAPP_BOX reference a
# fare configuration stores. One field — the API key from the Snapp Box panel,
# pasted as-is. The adapter sends it as the Authorization header verbatim, with
# no "Bearer " prefix, which is what Snapp's own client does.
DELIVERY_FARE_SNAPP_BOX=
# Where the Snapp adapter asks. Empty means the real service; Snapp's staging
# origin is https://customer-stg.snapp-box.com
SNAPP_BOX_ENDPOINT=
# What Snapp's totalFare is denominated in. Defaults to RIAL.
#
# RIAL against TOMAN is a factor of ten. Place one real order and reconcile the
# fare against the invoice Snapp issues BEFORE marking the configuration
# HEALTHY — until it is HEALTHY nothing is quoted through it, which is what
# makes that check safe to do.
SNAPP_BOX_AMOUNT_UNIT=
# The city and vehicle class in Snapp's own vocabulary, which only Snapp
# defines. GET /v2/delivery-category/by-city lists what your account may use.
#
# There is no sensible default and none is invented: without both, the adapter
# declines to quote and the published tariff prices the order.
SNAPP_BOX_CITY=
SNAPP_BOX_DELIVERY_CATEGORY=

# ---------------------------------------------------------------------------
# Legal identity, published on /legal/*
# ---------------------------------------------------------------------------
# Iranian e-commerce rules, and the eNamad review that enforces them, require a
# shop to publish who is behind it. These are facts about whoever deployed this,
# not about the code, so they live here. Until they are set the legal pages say
# plainly that the section is unfinished and name these variables — a plausible
# fake address is a false statement about a real business, and worse than blank.
LEGAL_ENTITY_NAME_FA=
LEGAL_TRADING_NAME_FA=
# شناسهٔ ملی for a company, or کد ملی for a sole trader.
LEGAL_NATIONAL_ID=
LEGAL_REGISTRATION_NUMBER=
# A street address somebody could visit, not a PO box.
LEGAL_ADDRESS_FA=
LEGAL_POSTAL_CODE=
# A number a person actually answers; an eNamad reviewer will dial it.
LEGAL_SUPPORT_PHONE=
LEGAL_SUPPORT_EMAIL=
LEGAL_SUPPORT_HOURS_FA=
# The eNamad seal, from the panel after the domain is approved. Both halves are
# required: a badge rendered from a guessed code claims a regulator verified
# this business when it has not, so a half-configured deployment shows nothing.
ENAMAD_ID=
ENAMAD_CODE=
SAMANDEHI_ID=

# Absolute origin this deployment serves from, e.g. https://www.alonon.ir. Read by
# robots.txt, the sitemap, and Open Graph metadata. Falls back to localhost
# rather than failing to boot: a missing value here is a metadata problem, not a
# reason for the storefront to stop rendering.
WEB_BASE_URL=http://localhost:3000

# ---------------------------------------------------------------------------
# Web push (RFC 8292). How the shop, added to a home screen, is told that the
# bread is at the door — and on an iPhone the only way it can ever be told,
# because Apple does not accept Iranian developer enrolments and there is no App
# Store build. Every message this replaces is a paid text message.
#
# Generate a pair with `pnpm --filter @alo-noon/api vapid-keys`.
#
# All three together or none: the configuration schema refuses a partial set. A
# public key with no private key makes the shop ask every customer for
# notification permission and then send nothing, and on most browsers a
# permission once refused cannot be asked about again — so the cost of getting
# this half-right is paid by customers, and setting the missing half tomorrow
# does not get them back.
#
# Not per-tenant, unlike the payment and SMS credentials above: this identifies
# the server to Google, Mozilla and Apple rather than identifying a shop to a
# vendor it has an account with. Nothing is billed and a tenant configures
# nothing.
#
# Rotating the pair is not routine. Every subscription a browser made is bound
# to the key it subscribed with, so a new pair stops all of them working while
# the customers who granted permission are never re-prompted — they simply stop
# hearing anything.
WEB_PUSH_VAPID_PUBLIC_KEY=
WEB_PUSH_VAPID_PRIVATE_KEY=
# A mailto: or https: URI a push service operator could reach. When one decides
# this server is misbehaving, this address is the alternative to being blocked
# without being told.
WEB_PUSH_SUBJECT=
