# Alo Noon pnpm configuration

# Enable strict mode for peer dependencies
auto-install-peers = true

# Shamefully hoist for workspace compatibility
shamefully-hoist = true

# Use independent versions per package
use-workspace-specifier = true

# Enable saving exact versions
save-exact = true
